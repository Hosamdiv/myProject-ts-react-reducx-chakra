import { Box } from "@chakra-ui/react";
import { ProdctCard } from "./ProductCard";

const Products = () => {
  return (
    <Box margin={30}>
      <ProdctCard />
    </Box>
  );
};

export default Products;
